import { StyledHeader } from "../styles/Header.styled";

const Header = () => {
  return (
    <StyledHeader color="gold" backgroundColor="navy">
      <h2>Header</h2>
    </StyledHeader>
  );
};
export default Header;
