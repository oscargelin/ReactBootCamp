import { StyledButton } from "../styles/Button.styled";

const Button = () => {
  return (
    <StyledButton backgroundColor="orange" color="green">
      Click me
    </StyledButton>
  );
};
export default Button;
